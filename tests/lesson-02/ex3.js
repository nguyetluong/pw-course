const a= true;
const b = false;
const c = true;
console.log(a&&b&&c); //false do toán tử && chỉ true khi tất cả các giá trị đều true
console.log(a&&b||c); //true do toán tử || chỉ cần 1 vế true là đủ

